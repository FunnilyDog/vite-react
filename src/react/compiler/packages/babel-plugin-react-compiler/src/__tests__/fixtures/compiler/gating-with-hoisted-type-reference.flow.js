//       @gating
import {memo} from 'react';

                                                   

component Component(value        ) {
  return <div>{value}</div>;
}

export default memo       (Component);
